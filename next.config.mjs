/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: ["lh3.googleusercontent.com", "res.cloudinary.com", "images.unsplash.com", "cdn.pixabay.com", "cdn.statically.io"]
    }
};

export default nextConfig;
